# ---- Global ----
PROJECT_NAME=mafia_like

# ---- Image tags (CI/CD에서 바꾸기 쉬움) ----
CADDY_IMAGE=caddy:2
POSTGRES_IMAGE=postgres:16
REDIS_IMAGE=redis:7
BE_IMAGE=ghcr.io/<org>/<repo>-be
BE_IMAGE_TAG=dev

# local 개발에서만 next dev 컨테이너를 쓸 경우(선택)
FE_IMAGE=ghcr.io/<org>/<repo>-fe
FE_IMAGE_TAG=dev

# ---- Ports exposed to host ----
HTTP_PORT=80
HTTPS_PORT=443
POSTGRES_PORT=5432
REDIS_PORT=6379
FE_DEV_PORT=3000
BE_PORT=8000

# ---- Domain / TLS (prod에서 주로 사용) ----
PUBLIC_DOMAIN=example.com
TLS_EMAIL=admin@example.com
