# Clarification: 방/로비 API 연동 및 라우팅 (MVP)

> 생성일: 2026-03-14
> 대상 스펙: spec.md
> 총 질문 수: 3

---

## Q1: 호스트가 방을 나갔을 때의 처리 방식

**카테고리**: 엣지 케이스 & 장애 처리

**맥락**: 스펙의 엣지 케이스 섹션에서 "호스트가 방을 나가면? → [확인 필요: 호스트 이탈 시 호스트 권한이 이전되는지, 방이 해산되는지]"로 명시적으로 미결 상태로 표시되어 있다. 백엔드의 Room 모델에서 `host_id`는 nullable(`UUID | None`)이며, SSE 이벤트에 `ROOM_DELETED` 코드가 존재한다.

**질문**: 호스트가 방을 떠났을 때 (A) 다른 참가자에게 호스트 권한이 자동 이전되는가, (B) 방이 해산(삭제)되는가, (C) 호스트 없이 방이 유지되는가?

**답변**:

---

## Q2: SSE 연결 지속 실패 시 사용자 행동 옵션

**카테고리**: 인터랙션 & UX 흐름

**맥락**: 스펙의 인수 시나리오 2-3에서 "자동으로 재연결을 시도한다"고 명시하고, 엣지 케이스에서 "지속 실패 시 에러 상태 표시"라고 되어 있다. 그러나 에러 상태에서 사용자가 취할 수 있는 행동이 정의되어 있지 않다.

**질문**: SSE 재연결이 지속적으로 실패하여 에러 상태가 표시된 후, 사용자에게 (A) 수동 재연결 버튼만 제공하는가, (B) 로비로 돌아가기 옵션을 제공하는가, (C) 둘 다 제공하는가?

**답변**:

---

## Q3: 강제 퇴장 시 알림 및 전환 방식

**카테고리**: 인터랙션 & UX 흐름

**맥락**: FR-008에서 "강제 퇴장당한 유저는 실시간으로 퇴장 알림을 받고 로비로 이동해야 한다"고 명시되어 있다. 그러나 "알림"의 형태와 로비 이동 타이밍이 불명확하다. 사용자가 알림을 확인해야 이동하는지, 자동으로 이동하면서 알림을 보여주는지에 따라 구현이 달라진다.

**질문**: 강제 퇴장 시 (A) 즉시 로비로 자동 이동하면서 토스트/배너로 알림을 표시하는가, (B) 모달로 퇴장 사실을 알리고 사용자가 확인한 후 로비로 이동하는가?

**답변**:
