# 구현 계획: [기능명]

## 요약

[기능 스펙에서 추출: 핵심 요구사항 + 리서치를 통한 기술적 접근 방식]

## 프로젝트 구조

### 생성/수정 파일 목록

<!--
  이 기능에서 생성하거나 수정할 파일을 아래 트리에 표시하세요.
  - 새로 생성하는 파일은 (new), 수정하는 파일은 (mod)로 표기
  - 해당 기능과 무관한 디렉토리/파일은 생략
-->

```text
# 예시 — 실제 생성/수정 파일로 교체할 것
src/
├── app/
│   └── rooms/
│       ├── page.tsx                (new)  # 방 목록 페이지
│       └── [roomId]/
│           └── page.tsx            (new)  # 방 상세 페이지
├── features/
│   └── room/
│       ├── room-list.tsx           (new)
│       └── room-card.tsx           (new)
├── shared/
│   └── lib/
│       └── use-room-sse.ts        (new)  # SSE 연결 훅
└── middleware.ts                   (mod)  # 인증 경로 추가
```

## 설계 결정

> 스펙에서 명확하지 않거나 여러 방법이 가능하여 구현을 위해 판단한 항목을 기록한다.
> 판단이 필요한 항목이 없으면 이 섹션 전체를 생략한다.

| 결정 사항 | 선택한 방식 | 근거 |
| --------- | ----------- | ---- |
| [예: SSE 재연결 전략] | [exponential backoff, 최대 5회] | [서버 부하 방지, UX 타협점] |
| [예: 폼 상태 관리] | [React Hook Form] | [기존 코드 일관성, Zod 연동] |
